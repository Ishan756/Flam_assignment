{
  "name": "study-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend Internship Assignment — Study Assistant. Turns notes into interactive flashcards and quizzes via an LLM.",
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -k -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
